# --- Database Configuration (Supabase Transaction Pooler) ---
# URL atualizada para a nova região (us-west-2) e novo pooler
spring.datasource.url=jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres

spring.datasource.username=postgres.ewjfeufkncanfcoloqge

spring.datasource.password=nqucebPnkHEW1cIJ

# Driver do PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA Configuration ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.hikari.data-source-properties.prepareThreshold=0

# Configurações de SSL para evitar erros de conexão segura
spring.datasource.hikari.data-source-properties.ssl=true
spring.datasource.hikari.data-source-properties.sslfactory=org.postgresql.ssl.NonValidatingFactory